# Khoi tao git va push code len github cua nhom
git init
git branch -M main

# Sua link github nhom vao day
# git remote add origin <link-repo>

git add .
git commit -m "initial commit"
# git push -u origin main

# Lenh checkout nhanh de lam viec nhom
# git checkout -b <ten_branch>
# git push -u origin <ten_branch>
